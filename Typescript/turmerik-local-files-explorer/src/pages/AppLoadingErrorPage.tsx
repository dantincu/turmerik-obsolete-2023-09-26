import Container from '@mui/material/Container';

const AppLoadingErrorPage = () => {
  return (
    <Container maxWidth="xl">
      <h1>Error... an error has occurred while trying to initialize the app</h1>
    </Container>);
}

export default AppLoadingErrorPage;